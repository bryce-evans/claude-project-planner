[project]
name = "claude-project-planner"
version = "0.1.0"
description = "Structured project setup, AI-assisted planning, and parallel agent execution"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
