> Generated by planning/plan.py. Edit manually or re-run the planner to regenerate.

# Plan

## Workstreams

| ID | Name | Scope | Status |
|----|------|-------|--------|
| WS1 | Bedrock | Owns local SQLite schema, record storage, migrations, and the AI query engine — does not own UI or voice input. | todo |
| WS2 | Songbird | Owns all voice capture, OpenAI Realtime API integration, live field-extraction, and record assembly — does not own persistence or UI chrome. | todo |
| WS3 | Folio | Owns all screens, navigation, schema selector, live checklist UI, record albums view, and camera attachment — does not own data or voice logic. | todo |


## WS1 — Bedrock

**Scope:** Owns local SQLite schema, record storage, migrations, and the AI query engine — does not own UI or voice input.

| Task | Priority | Estimate | Blockers | Status |
|------|----------|----------|----------|--------|
| Initialize Expo project with TypeScript and install core dependencies (expo-sqlite, zustand, expo-router, react-native-paper) | P0 | 2h | — | todo |
| Define SQLite schema for schemas table, records table, and record_fields table; write migration runner | P0 | 4h | — | todo |
| Implement SchemaRepository: CRUD for JSON schema definitions with two hardcoded MVP schemas pre-seeded | P0 | 4h | SQLite migration runner | todo |
| Implement RecordRepository: insert, fetch-all, fetch-by-schema, and attach-photo path methods | P0 | 4h | SQLite migration runner | todo |
| Expose Zustand store slices for activeSchema and recordDraft so WS2 and WS3 can read/write shared state | P0 | 2h | SchemaRepository, RecordRepository | todo |
| Build AI query engine: accept natural-language question, call GPT-4o to generate SQLite query, execute it, return formatted answer | P1 | 1d | RecordRepository | todo |
| Add auto-fill service: capture timestamp, GPS via expo-location, and weather from OpenWeatherMap at record-creation time | P1 | 4h | RecordRepository | todo |
| Write integration tests covering schema seed, record insert/retrieve, and a sample AI query round-trip | P2 | 4h | AI query engine | todo |


## WS2 — Songbird

**Scope:** Owns all voice capture, OpenAI Realtime API integration, live field-extraction, and record assembly — does not own persistence or UI chrome.

| Task | Priority | Estimate | Blockers | Status |
|------|----------|----------|----------|--------|
| Set up OpenAI Realtime API client and WebSocket connection for voice streaming | P0 | 4h | — | todo |
| Implement microphone capture and audio streaming pipeline via Expo Audio | P0 | 4h | — | todo |
| Build live transcription handler that emits partial and final transcript events | P0 | 4h | Set up OpenAI Realtime API client and WebSocket connection for voice streaming | todo |
| Implement field-extraction engine: parse transcript against active schema using GPT-4o function-calling | P0 | 1d | Build live transcription handler that emits partial and final transcript events, WS1 | todo |
| Emit live field-update events to Zustand store as fields resolve during speech | P1 | 4h | Implement field-extraction engine: parse transcript against active schema using GPT-4o function-calling | todo |
| Detect "done" utterance and trigger record-assembly finalization | P1 | 2h | Implement field-extraction engine: parse transcript against active schema using GPT-4o function-calling | todo |
| Assemble completed record object from resolved fields and hand off to WS1 persistence layer | P1 | 2h | Emit live field-update events to Zustand store as fields resolve during speech, WS1 | todo |
| Build query-mode voice session: capture question, send to GPT-4o for SQL generation, return answer as speech | P1 | 1d | WS1 | todo |
| Handle mic permissions, background interruptions, and session cleanup gracefully | P1 | 4h | Implement microphone capture and audio streaming pipeline via Expo Audio | todo |


## WS3 — Folio

**Scope:** Owns all screens, navigation, schema selector, live checklist UI, record albums view, and camera attachment — does not own data or voice logic.

| Task | Priority | Estimate | Blockers | Status |
|------|----------|----------|----------|--------|
| Set up Expo Router navigation shell with tab layout (Record, Albums, Query tabs) | P0 | 2h | — | todo |
| Implement schema selector screen with two hardcoded MVP schemas and Zustand active-schema state | P0 | 4h | — | todo |
| Build live checklist UI component consuming Zustand field-completion state from WS2 | P1 | 4h | WS2 | todo |
| Wire record creation screen: big button triggers WS2 voice session, renders live checklist, handles "done" signal | P1 | 1d | WS2, WS1 | todo |
| Build albums screen: list all records from WS1 SQLite, display fields in book-like card layout | P1 | 4h | WS1 | todo |
| Implement record detail view with field values and attached photo display | P1 | 4h | WS1 | todo |
| Add camera/gallery attachment via expo-image-picker on record creation screen | P1 | 2h | WS1 | todo |
| Build query screen: voice or text input, display WS1 AI answer in readable format | P1 | 4h | WS1, WS2 | todo |
